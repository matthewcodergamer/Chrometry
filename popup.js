const $=id=>document.getElementById(id);$('pick').addEventListener('click',async()=>{ $('status').textContent='Activating dropper…'; const r=await chrome.runtime.sendMessage({type:'CHROMETRY_START_PICKER'}); if(!r?.ok)$('status').textContent=r?.error||'Could not start picker.';else window.close(); });$('web').addEventListener('click',()=>chrome.runtime.sendMessage({type:'CHROMETRY_OPEN_WEB'}));