const {json,cors,bearer,activeSubscription,sign}=require('./_shared');
module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='GET')return json(res,405,{error:'Method not allowed.'});try{const {payload,subscription,active}=await activeSubscription(bearer(req),true);const token=active?sign({v:1,sub:payload.sub,customer:payload.customer,email:payload.email||null,exp:Math.floor(Date.now()/1000)+60*60*24*30}):null;return json(res,200,{active,email:payload.email||null,status:subscription.status,current_period_end:subscription.current_period_end||null,token});}catch(e){return json(res,401,{active:false,error:e.message||'License verification failed.'});}};
