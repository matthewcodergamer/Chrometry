const {json,cors,bearer,activeSubscription,stripe}=require('./_shared');
module.exports=async(req,res)=>{cors(res);if(req.method==='OPTIONS')return res.status(204).end();if(req.method!=='POST')return json(res,405,{error:'Method not allowed.'});try{const {payload,active}=await activeSubscription(bearer(req));if(!active)return json(res,402,{error:'Your Pro subscription is not active.'});const app=process.env.CHROMETRY_APP_URL;if(!app)return json(res,500,{error:'CHROMETRY_APP_URL is not configured.'});const portal=await stripe().billingPortal.sessions.create({customer:payload.customer,return_url:app});return json(res,200,{url:portal.url});}catch(e){return json(res,401,{error:e.message||'Unable to open billing portal.'});}};
